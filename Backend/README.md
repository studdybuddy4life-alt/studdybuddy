# StudyBuddy Backend

This is the backend for StudyBuddy — tutoring booking and subscription platform.

## Quick start (local)

1. Copy `.env.example` to `.env` and set values.
2. Run `npm install`
3. Run `node server.js` or `npm run dev` (with nodemon)

## Routes
- /auth
- /booking
- /payment
- /referral
- /subscription
- /subscription-usage

See code for details.
